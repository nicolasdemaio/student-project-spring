*Student:
    id, firstname, lastname, email, gender, birtdaydate

*Professor:
    id, firstName, lastname, birthdaydate

* Lombok: Libreria que permite atraves de anotaciones la creacion de setters, getters, constructores y mas
            para evitar el codigo repetitivo (boilerplate code).
               @Getters, @Setters, @NoArgsConstructor son algunas de las anotaciones.

* Usar anotaciones de JPA Entity como @NotNull, establecer valores por defecto, formato en las fechas, etc.

*Gender en alumno es un Enumtype, buscar en JPA como se hace para trasladarlo a bd.

*Course Entity TDD:
cuando es creado no tiene alumnos y su cupo es el q tiene
cuando se agrega un alumno, lo tiene y baja su cupo
cuando se agrega un alumno que ya esta cursando, error
cuando se intenta agregar un alumno y no hay cupos, error.

*Course Service debe mandar mail cuando se concreta una inscripcion. al mail del alumno.

